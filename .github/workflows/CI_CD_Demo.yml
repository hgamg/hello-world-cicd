name: "Demo Pipline"

on: [push]

jobs:
  release:
    name: release Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "release"
  Build:
    needs: release
    name: Build Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build"

  Test:
    needs: [release, Build]
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test"

