name: "Demo Pipline"

on: [push]

jobs:
  Build:
    name: Build Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build"
  Deploy:
    needs: Build
    name: Deploy Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy"

  Test:
    needs: [Build, Deploy]
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test"

