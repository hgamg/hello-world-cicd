name: "Demo Pipline"

on: [push]

jobs:
  release:
    name: release Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "release"
  Build:
    need: release
    name: Build Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build"

  Test:
    need: [release, Build]
    name: Test Job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test"

